// ======================================================================
// main.tsx（アプリのエントリーポイント）
// 役割：
// - ブラウザに React アプリを「起動」させる唯一の入口
// - #root 要素に App コンポーネントを描画する
// - React.StrictMode により開発時の問題検出を強化
// ======================================================================

import React from "react";
import ReactDOM from "react-dom/client";

// # アプリ本体（画面遷移・状態管理の司令塔）
import App from "./App";

// # グローバルCSS（index.css + App.css + 共通UI をまとめたもの）
import "./styles.css";

// ----------------------------------------------------------------------
// ReactDOM.createRoot
// - React 18 以降の新しい描画API
// - index.html の <div id="root"> にアプリをマウントする
// ----------------------------------------------------------------------
ReactDOM.createRoot(
  document.getElementById("root")!
).render(
  // --------------------------------------------------------------------
  // React.StrictMode
  // - 開発時のみ有効
  // - 副作用の検出・非推奨APIの警告などを強化
  // - 本番ビルドでは自動的に無効化される
  // --------------------------------------------------------------------
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
